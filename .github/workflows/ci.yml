# # This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# # For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

# name: Node.js CI

# on:
#   push:
#     branches: [ "main" ]

#   pull_request:
#     branches: [ "main" ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     env: 
#       PG_DATABASE_DEV_URL: ${{ secrets.DATABASE_URL }}

#     strategy:
#       matrix:
#         node-version: [lts/*]

#     steps:
#     - uses: actions/checkout@v4 # official Github Action that clones (checks out) your repo onto the runner/github server, making your project files available

#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v4
#       with:
#         node-version: ${{ matrix.node-version }}
#         cache: 'npm'
    
#     - name: Install dependencies 
#       run: npm ci
    
#     - name: run migrations
#       run:  npx drizzle-kit generate && npx drizzle-kit migrate
      
#     - name: seed database
#       run:  npm run db:seed

#     # - name: Run tests if test script is present
#     # - run: npm test --if-present

#     - name: Run build
#       run: npm run build 
  
